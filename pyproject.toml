[project]
name = "app-ux-agent"
version = "0.2.0"
description = "Natural-language UX validation agent for Android/iOS apps"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
